server-host: 192.168.1.100

spring:
  application:
    name: printer_agent

server:
  port: 18080

pop:
  server:
    url: ${POP_SERVER_URL:http://localhost:8081}

front:
  server:
    url: http://localhost:5173